name: Make Canary Release

on:
  push:
    branches:
      - 'canary'
    paths:
      - '.github/edit2MakeNewCanary'

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        submodules: recursive

    - name: JDK 17 Setup
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Shell Scripts Permissions
      run: chmod +x .github/*.sh

    - name: Changelog Generation
      run: .github/extractChanges.sh

    - name: Version Bump
      run: .github/bumpVersion.sh ${{ github.run_id }}

    - name: Gradlew Permissions
      run: chmod +x gradlew

    - name: Gradle Build
      run: ./gradlew assembleDebug

    - name: Magisk Module Generation
      run: |
        mkdir -p output
        cp app/build/outputs/apk/debug/PixelXpert.apk MagiskModBase/system/priv-app/PixelXpert/PixelXpert.apk
        cd MagiskModBase
        zip -r ../output/PixelXpert.zip *

    - name: Ensure fork URLs in manifests
      run: |
        for f in latestCanary.json latestVersion.json MagiskModuleUpdate_Xposed.json MagiskModuleUpdate_Full.json; do
          if [ -f "$f" ]; then
            sed -i 's|raw.githubusercontent.com/siavash79/PixelXpert|raw.githubusercontent.com/lars-martens/pixelxpert-updates|g' "$f"
            sed -i 's|github.com/siavash79/PixelXpert|github.com/lars-martens/PixelXpert-fork|g' "$f"
          fi
        done
        sed -i 's|raw.githubusercontent.com/siavash79/PixelXpert|raw.githubusercontent.com/lars-martens/pixelxpert-updates|g' MagiskModBase/module.prop

    - name: Upload Xposed Artifact to Actions
      uses: actions/upload-artifact@v4
      with:
        name: PixelXpert.zip
        path: output

    - name: Canary Binary Release
      uses: ncipollo/release-action@v1
      with:
        tag: 'canary_builds'
        name: 'Latest Canary (Fork)'
        omitBodyDuringUpdate: true
        prerelease: true
        allowUpdates: true
        replacesArtifacts: true
        draft: false
        artifacts: 'output/*.*'

    - name: Sync manifests to public updates repo
      run: |
        git clone https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/lars-martens/pixelxpert-updates.git /tmp/updates
        cd /tmp/updates
        git checkout canary
        cp $GITHUB_WORKSPACE/latestCanary.json .
        cp $GITHUB_WORKSPACE/MagiskModuleUpdate_Xposed.json .
        cp $GITHUB_WORKSPACE/MagiskModuleUpdate_Full.json .
        cp $GITHUB_WORKSPACE/CanaryChangelog.md .
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add -A
        git diff --cached --quiet || git commit -m "sync: update manifests from fork build"
        git push origin canary

    - name: Version Bump Commit
      uses: github-actions-x/commit@v2.9
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        push-branch: 'canary'
        commit-message: 'Version update: Release'
        force-add: 'true'
        files: MagiskModBase/module.prop app/build.gradle.kts latestVersion.json CanaryChangelog.md latestCanary.json MagiskModuleUpdate_Xposed.json MagiskModuleUpdate_Full.json
        name: GitHub
        email: PixelXpert@siava.sh
